// Minimal test to isolate component issues
export default function TestMinimalBuild() {
  return (
    <div>
      <h1>Minimal Test</h1>
      <p>This should work if basic components are fine</p>
    </div>
  )
} 